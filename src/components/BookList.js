import React from "react";
import Book from "./Book";

function BookList({ books }) {
  return (
    <div className="book-container">
      {/* map over the books and return a Book component for each book */}
    </div>
  );
}

export default BookList;
